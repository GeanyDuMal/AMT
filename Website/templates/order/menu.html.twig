{% extends 'base.html.twig' %}

{% block title %}Menu Commande{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/style/order/menu.css">

	{# Import #}
	<link rel='stylesheet' href="https://cdn.datatables.net/responsive/2.2.9/css/responsive.dataTables.min.css">
	<link rel="stylesheet" href="https://cdn.datatables.net/1.11.3/css/jquery.dataTables.min.css">
{% endblock %}

{% block javascripts %}
	<script src="/script/order/menuOrder.js"></script>
	<script src="/script/order/ajaxMenuOrder.js"></script>

	{# Import #}
	<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
	<script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>
	<script type="text/javascript" src="https://cdn.datatables.net/responsive/2.2.9/js/dataTables.responsive.min.js"></script>
{% endblock %}

{% block body %}
    	<div class="content">
		<div class="content-header">
			<h1>Liste des commandes</h1>
		</div>
		<div style="margin:5px">
			<a href="{{ path('orderCreate') }}" class="btn btn-success">Ajouter</a>
		</div>
		{% if message %}
			<div class="alert alert-dismissible alert-success fade show">
				<button type="button" class="btn-close" data-bs-dismiss="alert"></button>
				<strong>{{ message }}!</strong>
			</div>
		{% endif %}
		
		{% if orderList|length > 0 %}
		<table id="order_table" class="table table-striped">
			<thead>
				<tr class="table-primary">
					<th>ID</th>
					<th>Date et Heure</th>
					<th>Nom Client</th>
					<th>Login</th>
					<th>Type Client</th>
					<th>Montant</th>
					{% if is_granted("ROLE_TRESORIER") %}
						<th>Supprimer</th>
					{% endif %}
				</tr>
			</thead>
			<tbody>
			{% for order in orderList %}
				<tr>
					<td id="idCell">
						{{ order.id }}
					</td>
					<td>
						{{ order.orderedAt|date('d/m/Y à H:i:s') }}
					</td>
					{% if order.client %}
						<td>
							{{ order.client.name }} {{ order.client.firstName }}
						</td>
						<td >
							{{ order.client.login }}
						</td>
						<td>
							{{ order.client.clientType.name }}
						</td>
					{% else %}
						<td>
							Client non renseigné
						</td>
						<td></td>
						<td></td>
					{% endif %}
						<td>
							{{ montantOrder[order.id] }}€
						</td>
					{% if is_granted("ROLE_TRESORIER") %}
						<td>
							<a id="toDelete" class="btn btn-danger">X</a>
						</td>
					{% endif %}
					
				</tr>
			{% endfor %}
			
			</tbody>
		</table>
		{% else %}
			<div class="alert alert-info alert-dismissible fade show">
				<p>
					<strong>Info!</strong>
				</p> 
				<p>
					Il n'y a pas encore des Commandes.
				</p> 
				<button type="button" class="btn-close" data-bs-dismiss="alert"></button>
			</div>
		{% endif %}
	</div>
{% endblock %}
