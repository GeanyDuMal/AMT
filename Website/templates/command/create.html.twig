{% extends 'base.html.twig' %}

{% block title %}Effectuer une commande{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/style/bootstrap.css">
    <link rel="stylesheet" href="/style/command/createCommand.css">
{% endblock %}

{% block javascripts %}
    <script src="/script/command/createCommand.js"></script>
{% endblock %}

{% block body %}
    <form method="POST" class="flex_vertical" id="form_command">
        <div class="flex_horizontal centered_flex_horizontal block_all_product">
        {% for product in productList %}
            <div class="block_product flex_vertical ">
                <div class="flex_vertical">
                    <div>
                        {{ product.name }}
                    </div>
                    <div class="quantity_block">
                        Quantité dispo : <span class="quantity_value">{{ product.quantityStock }}</span>
                    </div>
                </div>
                <div>
                    <input type="number" name="quantity_product_{{ product.id }}" class="input_quantity" step="1" value="0" min="0">
                    <div class="block_button">
                        <div class="btn btn-primary btn_minus">-</div>
                        <div class="btn btn-primary btn_plus">+</div>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
        <div>
            
        </div>
        <div class="flex_vertical centered_flex_vertical">
            <select name="client_commande" id="">
                <option value="null">Choisir un Client</option>
                {% for client in clientList %}
                    <option value="{{ client.id }}">{{ client.name }} {{ client.firstName }} / {{ client.login }}</option>
                {% endfor %}
            </select>
            <button type="submit" class="btn-primary" >Valider la commande</button>
        </div>
    </form>
{% endblock %}
