{% extends 'base.html.twig' %}

{% block title %}
    Connexion
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="/style/connexion/signin.css">
{% endblock %}


{% block body %}

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class="vertical_centered_menu">
        <h1>Connexion</h1>
        <form action="{{ path("login") }}" method="post" id="form_login">
            <div class="input_text">
                <label for="username">Login :</label>
                <input type="text" name="_username" value="{{ lastUsername }}"autofocus>
            </div>
            <div class="input_text">
                <label for="password">Mot de Passe :</label>
                <input type="password" name="_password">
            </div>
            <div>
                <button type="submit" class="btn_valider">Se Connecter</button> {# a mettre plus tard onclick="return verifyInput()"#}
            </div>

            {# If you want to control the URL the user is redirected to on success
            <input type="hidden" name="_target_path" value="/account"/> #}

        </form>

        <div class="text_bottom_signin">
            Pas encore Client ? <a href="{{ path('signin') }}">Inscrivez vous</a>
        </div>
    </div>
{% endblock %}
